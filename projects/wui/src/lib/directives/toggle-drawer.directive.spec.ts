import { ToggleDrawerDirective } from './toggle-drawer.directive';

describe('ToggleDrawerDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleDrawerDirective();
    expect(directive).toBeTruthy();
  });
});
