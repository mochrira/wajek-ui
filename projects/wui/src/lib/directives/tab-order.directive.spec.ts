import { TabOrderDirective } from './tab-order.directive';

describe('TabOrderDirective', () => {
  it('should create an instance', () => {
    const directive = new TabOrderDirective();
    expect(directive).toBeTruthy();
  });
});
