import { DatefieldDirective } from './datefield.directive';

describe('DatefieldDirective', () => {
  it('should create an instance', () => {
    const directive = new DatefieldDirective();
    expect(directive).toBeTruthy();
  });
});
